From 3443930f66a789d07c1345e1b5cb1424f59e4c54 Mon Sep 17 00:00:00 2001
From: Paul Thomson <paulthomson@google.com>
Date: Fri, 17 Apr 2026 10:43:55 +0100
Subject: [PATCH 4/5] Target Java 17

For compatibility with Lint CLI running in Gradle.

See: ag/37869648
See: b/446964753#comment5
---
 java/java-indexing-impl/BUILD.bazel           |   1 +
 platform/analysis-api/BUILD.bazel             |   1 +
 .../CompletionItemLookupElement.java          |   2 +-
 .../codeInspection/ex/QuickFixWrapper.java    |   3 +-
 .../com/intellij/modcommand/ModCommand.java   |   8 +-
 .../modcommand/ModUpdateSystemOptions.java    |   4 +-
 platform/analysis-impl/BUILD.bazel            |   1 +
 .../BaseCompletionLookupArranger.java         |   4 +-
 .../completion/impl/CamelHumpMatcher.java     |   4 +-
 .../codeInsight/daemon/impl/Divider.java      |   4 +-
 .../daemon/impl/HighlightInfo.java            |  63 +++++-----
 .../RedundantSuppressInspectionBase.java      |   4 +-
 .../ModCommandBatchExecutorImpl.java          | 114 ++++++++++++------
 .../lang/impl/modcommand/PsiUpdateImpl.java   |   4 +-
 .../DependenciesBuilder.java                  |   2 +-
 .../ProjectInspectionProfileManager.kt        |   7 +-
 16 files changed, 132 insertions(+), 94 deletions(-)

diff --git a/java/java-indexing-impl/BUILD.bazel b/java/java-indexing-impl/BUILD.bazel
index 7ce8943a24a1..016754645c99 100644
--- a/java/java-indexing-impl/BUILD.bazel
+++ b/java/java-indexing-impl/BUILD.bazel
@@ -12,6 +12,7 @@ jvm_library(
   module_name = "intellij.java.indexing.impl",
   visibility = ["//visibility:public"],
   srcs = glob(["src/**/*.kt", "src/**/*.java", "src/**/*.form"], allow_empty = True),
+  kotlinc_opts = "@community//:k17",
   resources = [":indexing-impl_resources"],
   deps = [
     "//java/java-psi-api:psi",
diff --git a/platform/analysis-api/BUILD.bazel b/platform/analysis-api/BUILD.bazel
index 7ca63de8dfa5..aa2a686784c8 100644
--- a/platform/analysis-api/BUILD.bazel
+++ b/platform/analysis-api/BUILD.bazel
@@ -12,6 +12,7 @@ jvm_library(
   module_name = "intellij.platform.analysis",
   visibility = ["//visibility:public"],
   srcs = glob(["src/**/*.kt", "src/**/*.java", "src/**/*.form"], allow_empty = True),
+  kotlinc_opts = "@community//:k17",
   resources = [":analysis_resources"],
   deps = [
     "//jps/model-api:model",
diff --git a/platform/analysis-api/src/com/intellij/codeInsight/completion/CompletionItemLookupElement.java b/platform/analysis-api/src/com/intellij/codeInsight/completion/CompletionItemLookupElement.java
index 60af8d30fddb..825233189caa 100644
--- a/platform/analysis-api/src/com/intellij/codeInsight/completion/CompletionItemLookupElement.java
+++ b/platform/analysis-api/src/com/intellij/codeInsight/completion/CompletionItemLookupElement.java
@@ -152,7 +152,7 @@ public final class CompletionItemLookupElement extends LookupElement implements
     List<MarkupText.Fragment> fragments = mainText.fragments();
     String tailText = "";
     if (!fragments.isEmpty()) {
-      MarkupText.Fragment last = fragments.getLast();
+      MarkupText.Fragment last = fragments.get(fragments.size() - 1);
       if (last.kind() == MarkupText.Kind.GRAYED) {
         tailText = last.text();
         fragments = fragments.subList(0, fragments.size() - 1);
diff --git a/platform/analysis-api/src/com/intellij/codeInspection/ex/QuickFixWrapper.java b/platform/analysis-api/src/com/intellij/codeInspection/ex/QuickFixWrapper.java
index b571e2f0026e..5fb62d531174 100644
--- a/platform/analysis-api/src/com/intellij/codeInspection/ex/QuickFixWrapper.java
+++ b/platform/analysis-api/src/com/intellij/codeInspection/ex/QuickFixWrapper.java
@@ -279,8 +279,7 @@ public final class QuickFixWrapper implements IntentionAction, PriorityAction, C
         if (myDescriptor.getStartElement() == null) return null;
         ActionContext descriptorContext = ActionContext.from(myDescriptor);
         return myUnwrappedAction.getPresentation(
-          descriptorContext.withOffset(Math.clamp(context.offset(), descriptorContext.selection().getStartOffset(),
-                                                  descriptorContext.selection().getEndOffset())));
+          descriptorContext.withOffset(Math.max(descriptorContext.selection().getStartOffset(), Math.min(descriptorContext.selection().getEndOffset(), context.offset()))));
       }
       PsiElement psiElement = myDescriptor.getPsiElement();
       if (psiElement == null || !psiElement.isValid()) return null;
diff --git a/platform/analysis-api/src/com/intellij/modcommand/ModCommand.java b/platform/analysis-api/src/com/intellij/modcommand/ModCommand.java
index b84b0b278e8f..b60dff443144 100644
--- a/platform/analysis-api/src/com/intellij/modcommand/ModCommand.java
+++ b/platform/analysis-api/src/com/intellij/modcommand/ModCommand.java
@@ -84,7 +84,7 @@ public sealed interface ModCommand
     if (next.isEmpty()) return this;
     List<ModCommand> commands = new ArrayList<>(unpack());
     commands.addAll(next.unpack());
-    return commands.size() == 1 ? commands.getFirst() : new ModCompositeCommand(commands);
+    return commands.size() == 1 ? commands.get(0) : new ModCompositeCommand(commands);
   }
 
   /**
@@ -397,12 +397,12 @@ public sealed interface ModCommand
       if (sub instanceof ModUpdateFileText updateFileText && updateFileText.file().equals(virtualFile)) {
         offset = updateFileText.translateOffset(offset, leanRight);
       }
-      if (sub instanceof ModDeleteFile(VirtualFile fileToDelete) && fileToDelete.equals(virtualFile)) {
+      if (sub instanceof ModDeleteFile deleteFile && deleteFile.file().equals(virtualFile)) {
         // Navigation is useless: we are removing the target file
         return command;
       }
-      if (sub instanceof ModMoveFile(VirtualFile fileToMove, FutureVirtualFile targetFile) && fileToMove.equals(virtualFile)) {
-        virtualFile = targetFile;
+      if (sub instanceof ModMoveFile moveFile && moveFile.file().equals(virtualFile)) {
+        virtualFile = moveFile.targetFile();
       }
       if (!(sub instanceof ModNavigate)) {
         finalCommand = finalCommand.andThen(sub);
diff --git a/platform/analysis-api/src/com/intellij/modcommand/ModUpdateSystemOptions.java b/platform/analysis-api/src/com/intellij/modcommand/ModUpdateSystemOptions.java
index 8a6f98ca8660..004a29f3830e 100644
--- a/platform/analysis-api/src/com/intellij/modcommand/ModUpdateSystemOptions.java
+++ b/platform/analysis-api/src/com/intellij/modcommand/ModUpdateSystemOptions.java
@@ -32,8 +32,8 @@ public record ModUpdateSystemOptions(@NotNull List<@NotNull ModifiedOption> opti
 
   @Override
   public @NotNull ModCommand andThen(@NotNull ModCommand next) {
-    if (next instanceof ModUpdateSystemOptions(var nextOptions)) {
-      return new ModUpdateSystemOptions(ContainerUtil.concat(options, nextOptions));
+    if (next instanceof ModUpdateSystemOptions nextCmd) {
+      return new ModUpdateSystemOptions(ContainerUtil.concat(options, nextCmd.options()));
     }
     return ModCommand.super.andThen(next);
   }
diff --git a/platform/analysis-impl/BUILD.bazel b/platform/analysis-impl/BUILD.bazel
index 8f51e76fdd0c..f97b230eaa95 100644
--- a/platform/analysis-impl/BUILD.bazel
+++ b/platform/analysis-impl/BUILD.bazel
@@ -12,6 +12,7 @@ jvm_library(
   module_name = "intellij.platform.analysis.impl",
   visibility = ["//visibility:public"],
   srcs = glob(["src/**/*.kt", "src/**/*.java", "src/**/*.form", "gen/**/*.kt", "gen/**/*.java"], allow_empty = True),
+  kotlinc_opts = "@community//:k17",
   resources = [":analysis-impl_resources"],
   deps = [
     "//platform/analysis-api:analysis",
diff --git a/platform/analysis-impl/src/com/intellij/codeInsight/completion/BaseCompletionLookupArranger.java b/platform/analysis-impl/src/com/intellij/codeInsight/completion/BaseCompletionLookupArranger.java
index 75afb109eb59..71240fdbea37 100644
--- a/platform/analysis-impl/src/com/intellij/codeInsight/completion/BaseCompletionLookupArranger.java
+++ b/platform/analysis-impl/src/com/intellij/codeInsight/completion/BaseCompletionLookupArranger.java
@@ -163,7 +163,7 @@ public class BaseCompletionLookupArranger extends LookupArranger implements Comp
     if (pairs.isEmpty()) return true;
 
     for (int i = 1; i < pairs.size(); i++) {
-      if (!Comparing.equal(pairs.get(i).second, pairs.getFirst().second)) {
+      if (!Comparing.equal(pairs.get(i).second, pairs.get(0).second)) {
         return false;
       }
     }
@@ -661,7 +661,7 @@ public class BaseCompletionLookupArranger extends LookupArranger implements Comp
     if (exactMatches.isEmpty()) return null;
 
     if (exactMatches.size() == 1) {
-      return exactMatches.getFirst();
+      return exactMatches.get(0);
     }
 
     return sortByRelevance(groupItemsBySorter(exactMatches)).iterator().next();
diff --git a/platform/analysis-impl/src/com/intellij/codeInsight/completion/impl/CamelHumpMatcher.java b/platform/analysis-impl/src/com/intellij/codeInsight/completion/impl/CamelHumpMatcher.java
index d0f5fd8bbac6..93cfba9a54d2 100644
--- a/platform/analysis-impl/src/com/intellij/codeInsight/completion/impl/CamelHumpMatcher.java
+++ b/platform/analysis-impl/src/com/intellij/codeInsight/completion/impl/CamelHumpMatcher.java
@@ -59,7 +59,7 @@ public class CamelHumpMatcher extends PrefixMatcher {
     for (String s : CompletionUtil.iterateLookupStrings(element)) {
       @Nullable List<@NotNull MatchedFragment> ranges = myCaseInsensitiveMatcher.match(s);
       if (ranges == null) continue;
-      if (ranges.isEmpty() || skipUnderscores(s) >= ranges.getFirst().getStartOffset()) {
+      if (ranges.isEmpty() || skipUnderscores(s) >= ranges.get(0).getStartOffset()) {
         return true;
       }
     }
@@ -190,7 +190,7 @@ public class CamelHumpMatcher extends PrefixMatcher {
     if (underscoreEnd > 0) {
       List<MatchedFragment> ciRanges = myCaseInsensitiveMatcher.match(string);
       if (ciRanges != null && !ciRanges.isEmpty()) {
-        int matchStart = ciRanges.getFirst().getStartOffset();
+        int matchStart = ciRanges.get(0).getStartOffset();
         if (matchStart > 0 && matchStart <= underscoreEnd) {
           return myCaseInsensitiveMatcher.matchingDegree(string.substring(matchStart), true) - 1;
         }
diff --git a/platform/analysis-impl/src/com/intellij/codeInsight/daemon/impl/Divider.java b/platform/analysis-impl/src/com/intellij/codeInsight/daemon/impl/Divider.java
index b10003f11eb0..c12df7813f04 100644
--- a/platform/analysis-impl/src/com/intellij/codeInsight/daemon/impl/Divider.java
+++ b/platform/analysis-impl/src/com/intellij/codeInsight/daemon/impl/Divider.java
@@ -165,8 +165,8 @@ public final class Divider {
       }
     }
 
-    PsiElement parent = !outside.isEmpty() ? outside.getLast() :
-                        !inside.isEmpty() ? inside.getLast() :
+    PsiElement parent = !outside.isEmpty() ? outside.get(outside.size() - 1) :
+                        !inside.isEmpty() ? inside.get(inside.size() - 1) :
                         CollectHighlightsUtil.findCommonParent(root, startOffset, endOffset);
     while (parent != null && !(parent instanceof PsiFile)) {
       parent = parent.getParent();
diff --git a/platform/analysis-impl/src/com/intellij/codeInsight/daemon/impl/HighlightInfo.java b/platform/analysis-impl/src/com/intellij/codeInsight/daemon/impl/HighlightInfo.java
index 445c56cf40ef..2fb1bd82a28c 100644
--- a/platform/analysis-impl/src/com/intellij/codeInsight/daemon/impl/HighlightInfo.java
+++ b/platform/analysis-impl/src/com/intellij/codeInsight/daemon/impl/HighlightInfo.java
@@ -362,22 +362,24 @@ public class HighlightInfo implements Segment {
 
   @ApiStatus.Internal
   public final void addFileLevelComponent(@NotNull FileEditor fileEditor, @NotNull JComponent component) {
-    switch (fileLevelComponentsStorage) {
-      case null ->
-        fileLevelComponentsStorage = new Pair<>(fileEditor, component);
-      case Pair<?, ?> p -> {
-        //noinspection unchecked
-        Pair<FileEditor, JComponent> pair = (Pair<FileEditor, JComponent>)p;
-        Map<FileEditor, JComponent> map = new HashMap<>();
-        map.put(pair.first, pair.second);
-        map.put(fileEditor, component);
-        fileLevelComponentsStorage = map;
-      }
-      case Map<?, ?> map ->
-        //noinspection unchecked
-        ((Map<FileEditor, JComponent>)map).put(fileEditor, component);
-      default ->
-        LOG.error(new IllegalStateException("fileLevelComponents=" + fileLevelComponentsStorage));
+    Object storage = fileLevelComponentsStorage;
+    if (storage == null) {
+      fileLevelComponentsStorage = new Pair<>(fileEditor, component);
+    }
+    else if (storage instanceof Pair<?, ?>) {
+      //noinspection unchecked
+      Pair<FileEditor, JComponent> pair = (Pair<FileEditor, JComponent>)storage;
+      Map<FileEditor, JComponent> map = new HashMap<>();
+      map.put(pair.first, pair.second);
+      map.put(fileEditor, component);
+      fileLevelComponentsStorage = map;
+    }
+    else if (storage instanceof Map<?, ?>) {
+      //noinspection unchecked
+      ((Map<FileEditor, JComponent>)storage).put(fileEditor, component);
+    }
+    else {
+      LOG.error(new IllegalStateException("fileLevelComponents=" + storage));
     }
   }
 
@@ -398,21 +400,20 @@ public class HighlightInfo implements Segment {
 
   @ApiStatus.Internal
   public final @Nullable JComponent getFileLevelComponent(@NotNull FileEditor fileEditor) {
-    switch (fileLevelComponentsStorage) {
-      case null -> {
-        return null;
-      }
-      case Pair<?,?> pair -> {
-        return pair.first == fileEditor ? (JComponent)pair.second : null;
-      }
-      case Map<?, ?> map -> {
-        //noinspection unchecked
-        return ((Map<FileEditor, JComponent>)map).get(fileEditor);
-      }
-      default -> {
-        LOG.error(new IllegalStateException("fileLevelComponents=" + fileLevelComponentsStorage));
-        return null;
-      }
+    Object storage = fileLevelComponentsStorage;
+    if (storage == null) {
+      return null;
+    }
+    else if (storage instanceof Pair<?, ?> pair) {
+      return pair.first == fileEditor ? (JComponent)pair.second : null;
+    }
+    else if (storage instanceof Map<?, ?> map) {
+      //noinspection unchecked
+      return ((Map<FileEditor, JComponent>)map).get(fileEditor);
+    }
+    else {
+      LOG.error(new IllegalStateException("fileLevelComponents=" + storage));
+      return null;
     }
   }
 
diff --git a/platform/analysis-impl/src/com/intellij/codeInspection/RedundantSuppressInspectionBase.java b/platform/analysis-impl/src/com/intellij/codeInspection/RedundantSuppressInspectionBase.java
index d857be30d2fd..73284ff43bf8 100644
--- a/platform/analysis-impl/src/com/intellij/codeInspection/RedundantSuppressInspectionBase.java
+++ b/platform/analysis-impl/src/com/intellij/codeInspection/RedundantSuppressInspectionBase.java
@@ -172,8 +172,8 @@ public abstract class RedundantSuppressInspectionBase extends GlobalSimpleInspec
             Collection<String> suppressedIds = e.getValue();
             PsiElement suppressedScope = e.getKey();
             if (!suppressedIds.contains(toolId)) continue;
-            for (ProblemDescriptor descriptor : descriptors) {
-              if (!(descriptor instanceof ProblemDescriptor problemDescriptor)) continue;
+            for (ProblemDescriptor problemDescriptor : descriptors) {
+              if (!(problemDescriptor instanceof ProblemDescriptor)) continue;
               PsiElement element = problemDescriptor.getPsiElement();
               if (element == null) continue;
               PsiLanguageInjectionHost host = InjectedLanguageManager.getInstance(element.getProject()).getInjectionHost(element);
diff --git a/platform/analysis-impl/src/com/intellij/lang/impl/modcommand/ModCommandBatchExecutorImpl.java b/platform/analysis-impl/src/com/intellij/lang/impl/modcommand/ModCommandBatchExecutorImpl.java
index c082f6bbe7a3..772f5a293972 100644
--- a/platform/analysis-impl/src/com/intellij/lang/impl/modcommand/ModCommandBatchExecutorImpl.java
+++ b/platform/analysis-impl/src/com/intellij/lang/impl/modcommand/ModCommandBatchExecutorImpl.java
@@ -124,39 +124,66 @@ public class ModCommandBatchExecutorImpl implements ModCommandExecutor {
         command instanceof ModUpdateReferences || command instanceof ModOpenUrl) {
       return Result.INTERACTIVE;
     }
-    return switch (command) {
-      case ModLaunchEditorAction action -> action.optional() ? Result.NOTHING : Result.INTERACTIVE;
-      case ModRegisterTabOut ignored -> Result.NOTHING;
-      case ModUpdateFileText upd -> executeUpdate(project, upd) ? Result.SUCCESS : Result.ABORT;
-      case ModCreateFile create -> {
-        String message = executeCreate(project, create);
-        yield message == null ? Result.SUCCESS : new Error(message);
-      }
-      case ModDeleteFile deleteFile -> {
-        String message = executeDelete(deleteFile);
-        yield message == null ? Result.SUCCESS : new Error(message);
-      }
-      case ModMoveFile moveFile -> {
-        String message = executeMove(moveFile);
-        yield message == null ? Result.SUCCESS : new Error(message);
-      }
-      case ModCompositeCommand(var commands) -> {
-        BatchExecutionResult result = Result.NOTHING;
-        for (ModCommand subCommand : commands) {
-          result = result.compose(doExecuteInBatch(context, subCommand));
-          if (result == Result.ABORT || result instanceof Error) break;
-        }
-        yield result;
+
+    if (command instanceof ModLaunchEditorAction action) {
+      return action.optional() ? Result.NOTHING : Result.INTERACTIVE;
+    }
+    
+    if (command instanceof ModRegisterTabOut ignored) {
+      return Result.NOTHING;
+    }
+    
+    if (command instanceof ModUpdateFileText upd) {
+      return executeUpdate(project, upd) ? Result.SUCCESS : Result.ABORT;
+    }
+    
+    if (command instanceof ModCreateFile create) {
+      String message = executeCreate(project, create);
+      return message == null ? Result.SUCCESS : new Error(message);
+    }
+    
+    if (command instanceof ModDeleteFile deleteFile) {
+      String message = executeDelete(deleteFile);
+      return message == null ? Result.SUCCESS : new Error(message);
+    }
+    
+    if (command instanceof ModMoveFile moveFile) {
+      String message = executeMove(moveFile);
+      return message == null ? Result.SUCCESS : new Error(message);
+    }
+    
+    if (command instanceof ModCompositeCommand compositeCmd) {
+      var commands = compositeCmd.commands();
+      BatchExecutionResult result = Result.NOTHING;
+      for (ModCommand subCommand : commands) {
+        result = result.compose(doExecuteInBatch(context, subCommand));
+        if (result == Result.ABORT || result instanceof Error) break;
       }
-      case ModChooseAction chooser -> executeChooseInBatch(context, chooser);
-      case ModShowConflicts ignored -> Result.CONFLICTS;
-      case ModEditOptions<?> editOptions -> bypassEditOptions(editOptions, context);
-      case ModDisplayMessage(@NlsContexts.Tooltip String text, var kind) -> switch (kind) {
+      return result;
+    }
+    
+    if (command instanceof ModChooseAction chooser) {
+      return executeChooseInBatch(context, chooser);
+    }
+    
+    if (command instanceof ModShowConflicts ignored) {
+      return Result.CONFLICTS;
+    }
+    
+    if (command instanceof ModEditOptions<?> editOptions) {
+      return bypassEditOptions(editOptions, context);
+    }
+    
+    if (command instanceof ModDisplayMessage displayMessage) {
+      @NlsContexts.Tooltip String text = displayMessage.messageText(); 
+      var kind = displayMessage.kind();
+      return switch (kind) {
         case ERROR -> new Error(text);
         case INFORMATION -> Result.INTERACTIVE;
       };
-      default -> throw new IllegalArgumentException("Unknown command: " + command);
-    };
+    }
+    
+    throw new IllegalArgumentException("Unknown command: " + command);
   }
 
   private <T extends OptionContainer> BatchExecutionResult bypassEditOptions(@NotNull ModEditOptions<T> options, @NotNull ActionContext context) {
@@ -236,14 +263,16 @@ public class ModCommandBatchExecutorImpl implements ModCommandExecutor {
           return null;
         }
         VirtualFile newFile = parent.createChildData(this, file.getName());
-        if (create.content() instanceof ModCreateFile.Text(String text)) {
+        if (create.content() instanceof ModCreateFile.Text textContent) {
+          String text = textContent.text();
           PsiFile psiFile = PsiManager.getInstance(project).findFile(newFile);
           if (psiFile == null) return AnalysisBundle.message("modcommand.executor.unable.to.find.the.new.file", file.getName());
           Document document = psiFile.getViewProvider().getDocument();
           document.setText(text);
           PsiDocumentManager.getInstance(project).commitDocument(document);
         }
-        else if (create.content() instanceof ModCreateFile.Binary(byte[] bytes)) {
+        else if (create.content() instanceof ModCreateFile.Binary binaryContent) {
+          byte[] bytes = binaryContent.bytes();
           newFile.setBinaryContent(bytes);
         }
         return null;
@@ -363,12 +392,14 @@ public class ModCommandBatchExecutorImpl implements ModCommandExecutor {
                                                                modFile.newText(),
                                                                true));
       }
-      else if (command instanceof ModCreateFile(FutureVirtualFile createdFile, ModCreateFile.Content content)) {
+      else if (command instanceof ModCreateFile createFile) {
+        FutureVirtualFile createdFile = createFile.file();
+        ModCreateFile.Content content = createFile.content();
         if (content instanceof ModCreateFile.Directory) {
           createdDirs.add(getFileNamePresentation(project, createdFile));
         } else {
           String contentRepresentation =
-            content instanceof ModCreateFile.Text(String text) ? text : AnalysisBundle.message("preview.binary.content");
+            content instanceof ModCreateFile.Text textContent ? textContent.text() : AnalysisBundle.message("preview.binary.content");
           customDiffList.add(new IntentionPreviewInfo.CustomDiff(createdFile.getFileType(),
                                                                  getFileNamePresentation(project, createdFile),
                                                                  "",
@@ -391,7 +422,9 @@ public class ModCommandBatchExecutorImpl implements ModCommandExecutor {
       else if (command instanceof ModEditOptions<?> target) {
         return getEditOptionsPreview(context, target);
       }
-      else if (command instanceof ModDisplayMessage(String text, ModDisplayMessage.MessageKind kind)) {
+      else if (command instanceof ModDisplayMessage displayMessage) {
+        String text = displayMessage.messageText();
+        ModDisplayMessage.MessageKind kind = displayMessage.kind();
         if (kind == ModDisplayMessage.MessageKind.ERROR) {
           return new IntentionPreviewInfo.Html(new HtmlBuilder().append(
             AnalysisBundle.message("preview.cannot.perform.action")).br().append(text).toFragment(), IntentionPreviewInfo.InfoKind.ERROR);
@@ -400,15 +433,19 @@ public class ModCommandBatchExecutorImpl implements ModCommandExecutor {
           navigateInfo = new IntentionPreviewInfo.Html(text);
         }
       }
-      else if (command instanceof ModCopyToClipboard(String content)) {
+      else if (command instanceof ModCopyToClipboard copyToClipboard) {
+        String content = copyToClipboard.content();
         navigateInfo = new IntentionPreviewInfo.Html(text(
           AnalysisBundle.message("preview.copy.to.clipboard", StringUtil.shortenTextWithEllipsis(content, 50, 10))));
       }
-      else if (command instanceof ModOpenUrl(String url)) {
+      else if (command instanceof ModOpenUrl openUrl) {
+        String url = openUrl.url();
         navigateInfo = new IntentionPreviewInfo.Html(text(
           AnalysisBundle.message("preview.open.url", StringUtil.shortenTextWithEllipsis(url, 50, 10))));
       }
-      else if (command instanceof ModMoveFile(VirtualFile fileToCreate, FutureVirtualFile targetFile)) {
+      else if (command instanceof ModMoveFile moveFile) {
+        VirtualFile fileToCreate = moveFile.file();
+        FutureVirtualFile targetFile = moveFile.targetFile();
         IntentionPreviewInfo.Html html;
         if (targetFile.getName().equals(fileToCreate.getName())) {
           html = (IntentionPreviewInfo.Html)IntentionPreviewInfo.moveToDirectory(fileToCreate, targetFile.getParent());
@@ -417,7 +454,8 @@ public class ModCommandBatchExecutorImpl implements ModCommandExecutor {
         }
         fsActions.add(html.content());
       }
-      else if (command instanceof ModLaunchEditorAction(String id, boolean optional) && !optional) {
+      else if (command instanceof ModLaunchEditorAction action && !action.optional()) {
+        String id = action.actionId();
         AnAction anAction = ActionManager.getInstance().getAction(id);
         if (anAction != null) {
           fsActions.add(text(AnalysisBundle.message("preview.execute.action", anAction.getTemplateText())));
diff --git a/platform/analysis-impl/src/com/intellij/lang/impl/modcommand/PsiUpdateImpl.java b/platform/analysis-impl/src/com/intellij/lang/impl/modcommand/PsiUpdateImpl.java
index 32c5b15b70f1..402fe455a7d6 100644
--- a/platform/analysis-impl/src/com/intellij/lang/impl/modcommand/PsiUpdateImpl.java
+++ b/platform/analysis-impl/src/com/intellij/lang/impl/modcommand/PsiUpdateImpl.java
@@ -209,8 +209,8 @@ final class PsiUpdateImpl {
       }
       List<ModUpdateFileText.Fragment> intersected = myFragments.subList(insertionPoint, insertionPoint + intersect);
       if (!intersected.isEmpty()) {
-        ModUpdateFileText.Fragment first = intersected.getFirst();
-        ModUpdateFileText.Fragment last = intersected.getLast();
+        ModUpdateFileText.Fragment first = intersected.get(0);
+        ModUpdateFileText.Fragment last = intersected.get(intersected.size() - 1);
         int diff = intersected.stream().mapToInt(f -> f.newLength() - f.oldLength()).sum();
         ModUpdateFileText.Fragment
           mergedFragment = new ModUpdateFileText.Fragment(first.offset(), last.offset() + last.newLength() - diff - first.offset(),
diff --git a/platform/analysis-impl/src/com/intellij/packageDependencies/DependenciesBuilder.java b/platform/analysis-impl/src/com/intellij/packageDependencies/DependenciesBuilder.java
index d239f96543d3..9097afda41dd 100644
--- a/platform/analysis-impl/src/com/intellij/packageDependencies/DependenciesBuilder.java
+++ b/platform/analysis-impl/src/com/intellij/packageDependencies/DependenciesBuilder.java
@@ -124,7 +124,7 @@ public abstract class DependenciesBuilder {
           if (!getScope().contains(file)) { //exclude paths through scope
             final List<List<PsiFile>> paths = findPaths(file, to, processed);
             for (List<PsiFile> path : paths) {
-              path.addFirst(file);
+              path.add(0, file);
             }
             result.addAll(paths);
           }
diff --git a/platform/analysis-impl/src/com/intellij/profile/codeInspection/ProjectInspectionProfileManager.kt b/platform/analysis-impl/src/com/intellij/profile/codeInspection/ProjectInspectionProfileManager.kt
index c95a1ef54693..c9cdad6b2957 100644
--- a/platform/analysis-impl/src/com/intellij/profile/codeInspection/ProjectInspectionProfileManager.kt
+++ b/platform/analysis-impl/src/com/intellij/profile/codeInspection/ProjectInspectionProfileManager.kt
@@ -11,7 +11,6 @@ import com.intellij.configurationStore.hashElement
 import com.intellij.configurationStore.serializeObjectInto
 import com.intellij.configurationStore.unwrapState
 import com.intellij.configurationStore.wrapState
-import com.intellij.diagnostic.runActivity
 import com.intellij.openapi.Disposable
 import com.intellij.openapi.application.ApplicationManager
 import com.intellij.openapi.components.BaseState
@@ -106,10 +105,8 @@ open class ProjectInspectionProfileManager(final override val project: Project)
       return
     }
 
-    runActivity("project inspection profile loading") {
-      schemeManager.loadSchemes()
-      currentProfile.initInspectionTools(project)
-    }
+    schemeManager.loadSchemes()
+    currentProfile.initInspectionTools(project)
 
     StartupManager.getInstance(project).runAfterOpened {
       project.messageBus.syncPublisher(ProfileChangeAdapter.TOPIC).profilesInitialized()
-- 
2.54.0.545.g6539524ca2-goog

